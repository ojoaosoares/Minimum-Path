#ifndef MAP_HPP
#define MAP_HPP

#include <fstream>
#include <sstream>
#include <vector>
#include <string>

using namespace std;

typedef long long ll;

vector<vector<ll>> readMap(const string &pathfile);

#endif