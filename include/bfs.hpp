#ifndef BPS_HPP
#define BFS_HPP

#include <string>
#include <utility>
#include <vector>
#include <tuple>

typedef long long ll;

using namespace std;

pair<ll, vector<ll>> bfs(const vector<vector<char>> &map, ll x, ll y);

#endif